<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlinBang" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/apricot</item>
        <item name="colorSecondary">@color/horizon</item>
        <item name="android:statusBarColor">@color/east_bay</item>
    </style>

    <style name="Theme.PlinBang" parent="Base.Theme.PlinBang" />

</resources>